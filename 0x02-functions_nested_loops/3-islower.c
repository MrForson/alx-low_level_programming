#include "main.h"
/**
 * _islower-shows l if the input is a lowercase character, Another cases, shows 0
 * At c: The character in ASCII code
 * Return: 1 for lowercase character. 0 for the rest
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
{
return (i);
}
else
{
return (0);
}
_putchar('\n');
}
